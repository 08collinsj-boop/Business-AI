{
  "name": "business-ai-real-mvp",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify:local": "node scripts/verify-local.mjs",
    "verify": "npm test && npm run verify:local"
  }
}
